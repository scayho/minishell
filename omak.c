#include <stdio.h>
#include <readline/readline.h>
#include <readline/history.h>

int main(int ac, char   **arv)
{
    while (1)
    {
        readline(">> ");
    }
    // add_history("test");
    return (0);
}